---
phase: 02-holiday-data-api-integration-week-1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/types/holiday.ts
  - src/composables/useHolidays.spec.ts
  - src/components/calendar/DayCell.spec.ts
  - src/components/MunicipalitySelector.spec.ts
autonomous: true
requirements:
  - FR2-holiday-integration
  - FR2-municipality-selection

must_haves:
  truths:
    - "TypeScript compiler accepts holiday.offset and holiday.base on mobile holiday entries"
    - "All three Wave 0 spec files exist and run (even if all tests are pending/skipped)"
    - "npx vitest run exits zero after Wave 0"
  artifacts:
    - path: "src/types/holiday.ts"
      provides: "Extended Holiday interface with offset? and base? fields"
      contains: "offset?: number"
    - path: "src/composables/useHolidays.spec.ts"
      provides: "Test scaffold for composable — API fetch, fallback, municipality resolution"
    - path: "src/components/calendar/DayCell.spec.ts"
      provides: "Test scaffold for DayCell holiday class and title attribute"
    - path: "src/components/MunicipalitySelector.spec.ts"
      provides: "Test scaffold for combobox filter and configStore wiring"
  key_links:
    - from: "src/composables/useHolidays.ts (Wave 1)"
      to: "src/composables/useHolidays.spec.ts"
      via: "spec stubs describe the expected contract before implementation"
      pattern: "describe.*useHolidays"
    - from: "src/types/holiday.ts"
      to: "municipalities.json mobile entries"
      via: "offset? field allows TypeScript to compile resolveMunicipalHolidayDate"
      pattern: "offset\\?.*number"
---

<objective>
Fix the Holiday type mismatch that would cause TypeScript errors on mobile municipality entries, then scaffold all three Wave 0 spec files so every subsequent plan has an automated verify command that passes from the start.

Purpose: Unblocks parallel Wave 1 plans. Without the type fix, useHolidays.ts cannot compile. Without spec stubs, Wave 1 tasks have no automated verify target.
Output: Extended `holiday.ts`, three spec files with pending/skipped stubs, full suite green.
</objective>

<execution_context>
@/Users/arodri06/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arodri06/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-holiday-data-api-integration-week-1/02-CONTEXT.md
@.planning/phases/02-holiday-data-api-integration-week-1/02-RESEARCH.md
@.planning/phases/02-holiday-data-api-integration-week-1/02-VALIDATION.md

<interfaces>
<!-- Key existing types and contracts. No codebase exploration needed. -->

From src/types/holiday.ts (current — needs extension):
```typescript
export type HolidayType = 'fixed' | 'mobile';

export interface Holiday {
  id: string;
  name: string;
  day: number;
  month: number;
  type: HolidayType;
  description?: string;
}

export interface Municipality {
  id: string;
  name: string;
  district: string;
  holiday: Holiday;
}

export interface CalendarDay {
  date: Date;
  isWeekend: boolean;
  isHoliday: boolean;
  isVacation: boolean;
  holidayName?: string;
}
```

From src/composables/useCalendar.ts:
```typescript
export interface Day {
  date: Date
  dayOfMonth: number
  isWeekend: boolean
}
```

From vitest.config.ts (confirmed):
- environment: jsdom
- globals: true
- alias: @/ -> src/
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Holiday type for mobile municipality entries</name>
  <files>src/types/holiday.ts</files>
  <behavior>
    - Holiday interface with type='mobile' allows access to offset (number) and base (string) without TypeScript error
    - Holiday interface with type='fixed' still exposes day and month
    - Existing consumers of Holiday (municipality.holiday.day, municipality.holiday.month) continue to compile
  </behavior>
  <action>
    In `src/types/holiday.ts`, add two optional fields to the `Holiday` interface:
    - `offset?: number` — days offset from Easter for mobile holidays
    - `base?: string` — base event name (e.g. "easter") for mobile holidays

    Do NOT convert to a discriminated union — that would break existing consumers. The simpler optional-field approach is correct for Phase 2 (per research recommendation). The existing fields `day`, `month`, `id`, `description` remain unchanged.

    After the edit, run `npx tsc --noEmit` to confirm zero TypeScript errors across the project.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/utils/holiday-utils.spec.ts --reporter=verbose</automated>
  </verify>
  <done>TypeScript compiles cleanly; holiday-utils spec still passes (no regressions).</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 spec stubs</name>
  <files>
    src/composables/useHolidays.spec.ts,
    src/components/calendar/DayCell.spec.ts,
    src/components/MunicipalitySelector.spec.ts
  </files>
  <action>
    Create three spec files with pending/skipped test stubs. These stubs define the behavioral contract that Wave 1 implementations must satisfy — they fail RED (or are skipped) now and turn GREEN after Wave 1/2 implementation.

    **src/composables/useHolidays.spec.ts** — stubs for:
    - `useHolidays() returns national holidays from API (global:true only)` — mock fetch, verify 2026-01-01 in map
    - `useHolidays() falls back to local holidays when API fails` — mock fetch to reject, verify Carnaval date in map
    - `resolveMunicipalHoliday resolves fixed holiday to correct date` — Abrantes: 2026-06-14
    - `resolveMunicipalHoliday resolves mobile holiday to correct date` — Águeda easter+39: verify with getEaster

    Write as `it.todo('...')` stubs (Vitest's todo syntax — these are counted but skipped, suite exits 0).

    **src/components/calendar/DayCell.spec.ts** — stubs for:
    - `DayCell applies bg-red-100 text-red-700 for national holiday`
    - `DayCell applies bg-orange-100 text-orange-700 for municipal holiday`
    - `DayCell has title attribute with holiday name for national holiday`
    - `DayCell has title attribute with municipality name for municipal holiday`
    - `DayCell applies bg-pink-50 text-pink-600 for weekend non-holiday`

    Write as `it.todo('...')` stubs.

    **src/components/MunicipalitySelector.spec.ts** — stubs for:
    - `MunicipalitySelector filters municipalities by partial name match`
    - `MunicipalitySelector shows no results when query shorter than 2 chars`
    - `MunicipalitySelector updates configStore.selectedMunicipalityId on selection`
    - `MunicipalitySelector clears configStore.selectedMunicipalityId when input is cleared`

    Write as `it.todo('...')` stubs.

    Each file must have a top-level `describe` block and import the relevant source path (even if the file doesn't exist yet — Vitest will skip todo tests without requiring the import to resolve).

    Actually: to avoid import-not-found errors at this stage, do NOT import the not-yet-existing sources. Use `it.todo()` blocks inside `describe()` with a comment: `// Import will be added in Wave 1 when source file exists`.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All three spec files exist. `npx vitest run` exits with code 0. Output shows todo test counts (e.g., "13 todo"). Existing holiday-utils and useCalendar specs still pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 (no TypeScript errors)
- `npx vitest run` exits 0 (all existing tests pass, new stubs are todo)
- `src/types/holiday.ts` contains `offset?: number` and `base?: string`
- Three new spec files exist with `describe` blocks and `it.todo` entries
</verification>

<success_criteria>
TypeScript compiles cleanly with the extended Holiday type. Full test suite runs green with the new todo stubs in place. Wave 1 plans have valid automated verify targets.
</success_criteria>

<output>
After completion, create `.planning/phases/02-holiday-data-api-integration-week-1/02-01-SUMMARY.md`
</output>
