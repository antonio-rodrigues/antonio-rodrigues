---
phase: 04-persistence-and-polish-week-2
plan: "03"
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/components/calendar/DayCell.vue
  - README.md
  - .planning/STATE.md
autonomous: false
requirements:
  - UX-4.3
  - BUILD-4.3

must_haves:
  truths:
    - "Production build completes without errors"
    - "Interactive cells have distinct hover/focus feedback"
    - "README accurately describes setup and Portuguese context"
  artifacts:
    - path: "src/components/calendar/DayCell.vue"
      provides: "Subtle scale/shadow hover states for workdays"
    - path: "README.md"
      provides: "Final documentation"
---

<objective>
Execute final UI polish, build verification, and project documentation.
</objective>

<execution_context>
@/Users/arodri06/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add interactive hover/scale polish to DayCell.vue</name>
  <files>src/components/calendar/DayCell.vue</files>
  <action>
    Update `DayCell.vue` classes for workdays to include:
    `transition-all hover:scale-105 hover:shadow-sm z-10`.
    Ensure `z-10` on hover allows it to pop slightly without being cut by siblings.
  </action>
  <verify>
    <automated>Manual verification of hover effect.</automated>
  </verify>
  <done>Day cells feel interactive and responsive.</done>
</task>

<task type="auto">
  <name>Task 2: Final Build Verification</name>
  <files>dist/</files>
  <action>
    Run `npm run build`.
    Ensure no TypeScript errors from `vue-tsc`.
    Check that `dist/index.html` and assets exist.
  </action>
  <verify>
    <automated>Run shell command: npm run build</automated>
  </verify>
  <done>Production build is healthy.</done>
</task>

<task type="auto">
  <name>Task 3: Documentation and State Update</name>
  <files>README.md, .planning/STATE.md</files>
  <action>
    Update `README.md` to include:
    - Project Title: "Portugal Holiday & Vacation Planner"
    - Tech: Vue 3, Vite, Tailwind, Pinia
    - Features: Portuguese national/municipal holidays, vacation balance calculation, persistence.
    
    Mark Phase 4 as completed in `.planning/STATE.md`.
  </action>
  <verify>
    <automated>File exists and content is accurate.</automated>
  </verify>
  <done>Project is fully documented and phase is closed.</done>
</task>

</tasks>

<verification>
npm run build
npx vitest run --reporter=verbose
</verification>

<success_criteria>
- No console errors in production build.
- 100% test coverage for core logic (stats, holidays, persistence).
- State saved locally.
</success_criteria>
