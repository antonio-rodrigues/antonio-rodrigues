---
phase: 04-persistence-and-polish-week-2
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/config.ts
  - src/store/config.spec.ts
autonomous: true
requirements:
  - PERSISTENCE-4.1

must_haves:
  truths:
    - "Marked vacation days are saved to localStorage whenever they change"
    - "Selected municipality is saved to localStorage whenever it changes"
    - "Store initializes with state from localStorage if available"
    - "markedDays is correctly serialized/deserialized between Set and Array"
  artifacts:
    - path: "src/store/config.ts"
      provides: "localStorage sync via watch + initial state loading"
    - path: "src/store/config.spec.ts"
      provides: "Tests for persistence loading and saving"
---

<objective>
Implement state persistence for the Pinia config store using localStorage. Sync markedDays and selectedMunicipalityId.
</objective>

<execution_context>
@/Users/arodri06/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enable and fix persistence tests in config.spec.ts</name>
  <files>src/store/config.spec.ts</files>
  <action>
    Revert the `it.todo` changes in `src/store/config.spec.ts` back to `it`.
    Ensure the tests match the expected `STORAGE_KEY = 'calendar-vacation-planner-v1'`.
    Run `npm test src/store/config.spec.ts` to confirm they are failing.
  </action>
  <verify>
    <automated>npx vitest run src/store/config.spec.ts</automated>
  </verify>
  <done>Persistence tests are active and failing as expected.</done>
</task>

<task type="auto">
  <name>Task 2: Implement localStorage sync in useConfigStore</name>
  <files>src/store/config.ts</files>
  <action>
    In `src/store/config.ts`:
    1. Define `const STORAGE_KEY = 'calendar-vacation-planner-v1'`.
    2. Create a helper `loadInitialState()` that parses `localStorage.getItem(STORAGE_KEY)`.
    3. Initialize `selectedMunicipalityId` and `markedDays` with values from `loadInitialState()`.
    4. Use `watch` (from 'vue') to deep-watch the store state and save to `localStorage`.
    
    CRITICAL: `markedDays` is a `Set<string>`.
    - Serialization: `JSON.stringify({ ...state, markedDays: Array.from(markedDays.value) })`
    - Deserialization: `new Set(saved.markedDays || [])`
  </action>
  <verify>
    <automated>npx vitest run src/store/config.spec.ts</automated>
  </verify>
  <done>All 6 store tests (including persistence) are green. State syncs correctly.</done>
</task>

</tasks>

<verification>
npx vitest run src/store/config.spec.ts
</verification>

<success_criteria>
- Refreshing the browser preserves vacation marks and municipality.
- Pinia store handles Set <-> Array conversion for localStorage.
- All unit tests pass.
</success_criteria>
