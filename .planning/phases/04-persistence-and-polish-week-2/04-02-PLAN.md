---
phase: 04-persistence-and-polish-week-2
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/DashboardSidebar.vue
  - src/components/DashboardSidebar.spec.ts
  - src/components/MunicipalitySelector.vue
autonomous: true
requirements:
  - FEEDBACK-4.2
  - UX-4.2

must_haves:
  truths:
    - "Exceeding budget displays a prominent warning with icon"
    - "Transitions are present on stat values in the dashboard"
    - "Municipality dropdown transitions smoothly"
  artifacts:
    - path: "src/components/DashboardSidebar.vue"
      provides: "Alert banner + CSS transitions for numbers"
    - path: "src/components/DashboardSidebar.spec.ts"
      provides: "Tests for alert presence/absence based on budget"
    - path: "src/components/MunicipalitySelector.vue"
      provides: "Transition for results list"
---

<objective>
Implement budget alerts and visual transitions for stats and selectors.
</objective>

<execution_context>
@/Users/arodri06/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add alert and transition tests to DashboardSidebar.spec.ts</name>
  <files>src/components/DashboardSidebar.spec.ts</files>
  <action>
    Add a test case to `src/components/DashboardSidebar.spec.ts`:
    - Test: "displays 'Excedeu o seu saldo de férias!' when isOverBudget is true"
    - Test: "does not display warning when isOverBudget is false"
    Run tests to confirm failure.
  </action>
  <verify>
    <automated>npx vitest run src/components/DashboardSidebar.spec.ts</automated>
  </verify>
  <done>Alert tests are active and failing.</done>
</task>

<task type="auto">
  <name>Task 2: Implement alert banner and transitions in DashboardSidebar.vue</name>
  <files>src/components/DashboardSidebar.vue</files>
  <action>
    In `src/components/DashboardSidebar.vue`:
    1. Import `AlertTriangle` from 'lucide-vue-next'.
    2. Add a new banner at the bottom (or top) of the stat container that only shows when `isOverBudget` is true.
    3. Style with red background/text: `bg-red-50 text-red-700 border border-red-200 rounded-md p-3 mb-4`.
    4. Add a `Transition` around the stat numbers (using Tailwind `transition-all` or a custom CSS class).
  </action>
  <verify>
    <automated>npx vitest run src/components/DashboardSidebar.spec.ts</automated>
  </verify>
  <done>Alert appears correctly, tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add smooth transition to MunicipalitySelector dropdown</name>
  <files>src/components/MunicipalitySelector.vue</files>
  <action>
    In `src/components/MunicipalitySelector.vue`:
    1. Wrap the results list (`ul`) in a `<Transition>` component.
    2. Define transition classes (e.g., `enter-active-class="transition duration-100 ease-out" enter-from-class="transform scale-95 opacity-0" ...`).
  </action>
  <verify>
    <automated>Manual verification of dropdown animation.</automated>
  </verify>
  <done>Municipality dropdown has a smooth appearance animation.</done>
</task>

</tasks>

<verification>
npx vitest run src/components/DashboardSidebar.spec.ts
</verification>

<success_criteria>
- Alert Triangle and message appear when exceeding 22 days.
- Stats update smoothly.
- Dropdown doesn't flicker/jump.
</success_criteria>
