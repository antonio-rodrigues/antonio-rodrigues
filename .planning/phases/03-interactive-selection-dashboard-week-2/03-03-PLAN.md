---
phase: 03-interactive-selection-dashboard-week-2
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/components/DashboardSidebar.vue
  - src/components/DashboardSidebar.spec.ts
  - src/App.vue
autonomous: false
requirements:
  - TASK-3.2

must_haves:
  truths:
    - "User sees a summary panel showing vacation balance, used days, remaining days, and longest rest period"
    - "Stats update immediately when user clicks a day cell"
    - "Dashboard is readable on mobile (375px) — horizontal strip, not sidebar"
  artifacts:
    - path: "src/components/DashboardSidebar.vue"
      provides: "Presentational summary panel wired to useVacationStats"
      exports: []
    - path: "src/components/DashboardSidebar.spec.ts"
      provides: "Unit tests for correct stat rendering"
    - path: "src/App.vue"
      provides: "DashboardSidebar mounted between header and YearGrid"
  key_links:
    - from: "src/components/DashboardSidebar.vue"
      to: "src/composables/useVacationStats.ts"
      via: "useVacationStats(store.year, store.markedDays, holidays.value, store.maxVacationDays)"
      pattern: "useVacationStats"
    - from: "src/App.vue"
      to: "src/components/DashboardSidebar.vue"
      via: "<DashboardSidebar /> between header and main"
      pattern: "DashboardSidebar"
---

<objective>
Build DashboardSidebar component that displays real-time vacation stats, wire it into App.vue, and implement unit tests.

Purpose: Closes the user-facing loop — the click interaction from plan 01 and stats logic from plan 02 are now visible to the user in a summary panel.
Output: DashboardSidebar.vue component, full unit test coverage, App.vue updated to mount it.
</objective>

<execution_context>
@/Users/arodri06/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arodri06/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-interactive-selection-dashboard-week-2/03-01-SUMMARY.md
@.planning/phases/03-interactive-selection-dashboard-week-2/03-02-SUMMARY.md

<interfaces>
<!-- useVacationStats signature (from plan 02) -->
```typescript
import { useVacationStats } from '../composables/useVacationStats'
// Returns: { usedWorkDays, remainingDays, isOverBudget, longestRestPeriod }
// All are ComputedRef<number> except isOverBudget: ComputedRef<boolean>
```

<!-- useHolidays signature (existing) -->
```typescript
import { useHolidays } from '../composables/useHolidays'
const { holidays } = useHolidays()
// holidays: Ref<Map<string, HolidayEntry>>
```

<!-- useConfigStore (after plan 01 extension) -->
```typescript
const store = useConfigStore()
// store.year: Ref<number>
// store.markedDays: Ref<Set<string>>
// store.maxVacationDays: Ref<number>
```

<!-- App.vue current structure -->
// header (h1 + MunicipalitySelector)
// main (YearGrid)
// DashboardSidebar goes BETWEEN header and main
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build DashboardSidebar component with unit tests</name>
  <files>src/components/DashboardSidebar.vue, src/components/DashboardSidebar.spec.ts</files>
  <behavior>
    - Test: renders "Dias Usados" label and shows the usedWorkDays value
    - Test: renders "Dias Restantes" label and shows the remainingDays value
    - Test: renders "Maior Período de Descanso" label and shows longestRestPeriod value
    - Test: when isOverBudget is true, remaining days text has text-red-600 class
  </behavior>
  <action>
    Write tests FIRST in src/components/DashboardSidebar.spec.ts, replacing the it.todo stubs.

    For tests: mount DashboardSidebar with createPinia(); mock useVacationStats by providing a stub Pinia store with known markedDays/maxVacationDays values, or directly pass computed refs as props (prefer props if it simplifies testing).

    Component design decision: Accept stats as props (testable without mocking composables) OR call useVacationStats internally. Use props for the DashboardSidebar component — it makes unit testing trivial and keeps the component purely presentational:

    ```typescript
    // DashboardSidebar.vue props
    defineProps<{
      usedWorkDays: number
      remainingDays: number
      isOverBudget: boolean
      longestRestPeriod: number
      maxVacationDays: number
    }>()
    ```

    Template layout (horizontal strip, mobile-first):
    ```html
    <div class="max-w-7xl mx-auto px-4 mb-6">
      <div class="bg-white rounded-lg shadow-sm border border-gray-100 p-4 flex flex-wrap gap-6 justify-around">
        <!-- Stat: Saldo de Férias -->
        <div class="flex flex-col items-center">
          <span class="text-xs text-gray-500 uppercase tracking-wide">Saldo de Férias</span>
          <span class="text-2xl font-bold text-gray-900">{{ maxVacationDays }}</span>
          <span class="text-xs text-gray-400">dias totais</span>
        </div>
        <!-- Stat: Dias Usados -->
        <div class="flex flex-col items-center">
          <span class="text-xs text-gray-500 uppercase tracking-wide">Dias Usados</span>
          <span class="text-2xl font-bold text-gray-900">{{ usedWorkDays }}</span>
          <span class="text-xs text-gray-400">dias úteis</span>
        </div>
        <!-- Stat: Dias Restantes -->
        <div class="flex flex-col items-center">
          <span class="text-xs text-gray-500 uppercase tracking-wide">Dias Restantes</span>
          <span class="text-2xl font-bold" :class="isOverBudget ? 'text-red-600' : 'text-gray-900'">
            {{ remainingDays }}
          </span>
          <span class="text-xs text-gray-400">dias úteis</span>
        </div>
        <!-- Stat: Maior Período de Descanso -->
        <div class="flex flex-col items-center">
          <span class="text-xs text-gray-500 uppercase tracking-wide">Maior Período de Descanso</span>
          <span class="text-2xl font-bold text-gray-900">{{ longestRestPeriod }}</span>
          <span class="text-xs text-gray-400">dias consecutivos</span>
        </div>
      </div>
    </div>
    ```
  </action>
  <verify>
    <automated>npx vitest run src/components/DashboardSidebar.spec.ts</automated>
  </verify>
  <done>4 DashboardSidebar unit tests green. Component renders correct values. isOverBudget triggers text-red-600 on remaining days.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DashboardSidebar into App.vue</name>
  <files>src/App.vue</files>
  <action>
    In App.vue:
    1. Import DashboardSidebar from './components/DashboardSidebar.vue'
    2. Import useVacationStats from './composables/useVacationStats'
    3. Import useHolidays from './composables/useHolidays'
    4. Import useConfigStore from './store/config'
    5. Import { computed } from 'vue'

    In script setup:
    ```typescript
    const store = useConfigStore()
    const { holidays } = useHolidays()
    const { usedWorkDays, remainingDays, isOverBudget, longestRestPeriod } = useVacationStats(
      store.year,
      store.markedDays,
      holidays,
      store.maxVacationDays
    )
    ```

    Note: useConfigStore returns plain refs — pass store.year (a Ref<number>), not a computed wrapper.

    In template, place DashboardSidebar between </header> and <main>:
    ```html
    <DashboardSidebar
      :used-work-days="usedWorkDays"
      :remaining-days="remainingDays"
      :is-over-budget="isOverBudget"
      :longest-rest-period="longestRestPeriod"
      :max-vacation-days="store.maxVacationDays"
    />
    ```

    Do NOT change the header or YearGrid structure.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Full test suite passes (npx vitest run exits 0). DashboardSidebar is mounted in App.vue between header and main.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser verification of vacation toggle and dashboard</name>
  <what-built>
    - DashboardSidebar panel showing vacation stats below the header
    - DayCell green highlight on workday click
    - Real-time stat updates as days are toggled
  </what-built>
  <how-to-verify>
    1. Run: npm run dev — open http://localhost:5173
    2. Confirm the stats panel appears below the header (4 stat cards: Saldo, Usados, Restantes, Maior Período)
    3. Click any non-holiday, non-weekend day — it should turn green and "Dias Usados" should increment by 1
    4. Click the same day again — it should revert to white/gray and "Dias Usados" should decrement
    5. Click a red holiday cell — nothing should happen (no green, no counter change)
    6. Click a pink weekend cell — nothing should happen
    7. Hover over a holiday — cursor should be not-allowed; hover over a workday — cursor should be pointer
    8. Resize browser to 375px width — stats panel should remain readable (wraps to 2x2 or stacked layout)
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass, or describe which checks failed</resume-signal>
  <action>Manual browser verification — see how-to-verify steps above.</action>
  <verify>
    <automated>MANUAL — browser verification required (see how-to-verify)</automated>
  </verify>
  <done>User confirms all 8 browser checks pass and types "approved".</done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose

Full suite green (28 existing + ~13 new tests). DashboardSidebar renders correct reactive stats. App.vue mounts the panel without breaking existing layout.
</verification>

<success_criteria>
- DashboardSidebar.vue renders 4 stat panels using props
- Stats update reactively when vacation days are toggled
- isOverBudget turns remaining days red
- App.vue mounts DashboardSidebar between header and YearGrid
- Full Vitest suite green
- User-verified in browser (checkpoint passed)
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-selection-dashboard-week-2/03-03-SUMMARY.md`
</output>
