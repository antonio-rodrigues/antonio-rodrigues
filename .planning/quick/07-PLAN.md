# QT-16: Add background-color to #app

The user wants to add `background-color: darkmagenta;` to the `#app` selector in `src/style.css`.

## Goal
- [ ] Add `background-color: darkmagenta;` to `#app` in `src/style.css`.

## Proposed Changes
### src/style.css
- Add `background-color: darkmagenta;` within the `#app` rule.

## Verification
- [ ] Check if the property is correctly added to `src/style.css`.
- [ ] Ensure it's in the main `#app` selector, not just the media query one.
