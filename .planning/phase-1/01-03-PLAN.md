---
phase: 01-scaffolding-logic
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [src/components/calendar/MonthCard.vue, src/components/calendar/DayCell.vue, src/components/calendar/YearGrid.vue, src/App.vue]
autonomous: false
requirements: [1.3]
must_haves:
  truths:
    - "12 months are visible in a responsive grid"
    - "Each month shows days in a 7-column layout"
    - "Weekends are visually distinct"
    - "On desktop, grid is 3 or 4 columns wide"
    - "On mobile, grid is 1 column wide"
  artifacts:
    - path: "src/components/calendar/MonthCard.vue"
      provides: "Month-level rendering"
    - path: "src/components/calendar/YearGrid.vue"
      provides: "Annual calendar layout"
  key_links:
    - from: "src/components/calendar/YearGrid.vue"
      to: "src/composables/useCalendar.ts"
      via: "import and call"
      pattern: "useCalendar"
---

<objective>
Create a responsive 12-month calendar UI that adapts to different screen sizes, following the design of Calendarr Portugal.

Purpose: Provides the primary visual interface for the application.
Output: A set of Vue components for the annual calendar view.
</objective>

<execution_context>
@/Users/arodri06/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/arodri06/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phase-1/research/RESEARCH.md
@.planning/phase-1/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Month and Day Components</name>
  <files>src/components/calendar/DayCell.vue, src/components/calendar/MonthCard.vue</files>
  <action>
    Create `DayCell.vue`:
    - Display day number.
    - Style: light background, rounded corners.
    - Weekend styling: subtle gray/pink background.
    - Clickable area for future selection.

    Create `MonthCard.vue`:
    - Display month name (uppercase).
    - Header: MTWTFSS (Portuguese initials: S T Q Q S S D).
    - Grid: 7 columns for days.
    - Logic to add empty leading cells based on `firstDayOffset`.
  </action>
  <verify>
    <automated>test -f src/components/calendar/DayCell.vue && test -f src/components/calendar/MonthCard.vue</automated>
  </verify>
  <done>Month and Day components created with basic styling.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Responsive YearGrid</name>
  <files>src/components/calendar/YearGrid.vue, src/App.vue</files>
  <action>
    Create `YearGrid.vue`:
    - Call `useCalendar(2026)` to get month data.
    - Use Tailwind grid classes for responsive layout:
      - Mobile: `grid-cols-1`
      - Tablet: `md:grid-cols-2`
      - Desktop: `lg:grid-cols-3 xl:grid-cols-4`
    - Gap: `gap-6` or `gap-8`.

    Update `App.vue`:
    - Render `YearGrid` as the main content.
    - Add a simple page title "Planeador de Férias 2026".
  </action>
  <verify>
    <automated>test -f src/components/calendar/YearGrid.vue && grep "grid-cols-1" src/components/calendar/YearGrid.vue</automated>
  </verify>
  <done>Responsive year grid implemented and integrated into App.vue.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A responsive 12-month calendar grid for 2026.</what-built>
  <how-to-verify>
    1. Run `npm run dev`.
    2. Open browser.
    3. Verify that 12 months are visible.
    4. Verify that weekends are visually highlighted.
    5. Resize the window to test responsiveness (mobile vs desktop views).
    6. Check that Jan 1st 2026 is a Thursday (4th column).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
- 12 months rendered correctly for 2026.
- Weekends are clearly identifiable.
- Layout adapts smoothly from mobile (1 col) to large desktop (4 col).
- Correct weekday alignment (Jan 1, 2026 is a Thursday).
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding-logic/01-03-SUMMARY.md`
</output>
