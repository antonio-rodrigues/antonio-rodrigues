---
phase: 01-scaffolding-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tailwind.config.js, src/App.vue, src/types/holiday.ts, src/data/municipalities.json, src/store/config.ts]
autonomous: true
requirements: [1.1, 1.4]
must_haves:
  truths:
    - "Vue 3 project with Vite and Tailwind CSS is running"
    - "Holiday and Municipality types are defined"
    - "A sample list of municipalities is available in JSON"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies (vue, tailwindcss, date-fns, lucide, pinia)"
    - path: "src/types/holiday.ts"
      provides: "Type definitions for holidays and municipalities"
    - path: "src/data/municipalities.json"
      provides: "Initial data for 308 municipalities (sample for now)"
  key_links:
    - from: "src/App.vue"
      to: "Tailwind CSS"
      via: "Directives"
      pattern: "@tailwind base"
---

<objective>
Initialize the Vue 3 project environment with all necessary dependencies and define the core data models for holidays and municipalities.

Purpose: Provides the foundation for all future UI and logic development.
Output: Working project scaffold with Tailwind CSS, Pinia, and holiday data structures.
</objective>

<execution_context>
@/Users/arodri06/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/arodri06/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phase-1/research/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Initialization</name>
  <files>package.json, vite.config.ts, tailwind.config.js, postcss.config.js, src/main.ts, src/style.css, src/App.vue</files>
  <action>
    Initialize a new Vue 3 project with TypeScript using Vite. Install required dependencies:
    - tailwindcss, postcss, autoprefixer (for styling)
    - lucide-vue-next (for icons)
    - date-fns (for calendar math)
    - pinia (for state management)
    - axios (for future API calls)

    Configure Tailwind CSS with the standard 'npx tailwindcss init -p' and set the content paths.
    Clean up boilerplate App.vue and main.ts to a minimal state.
  </action>
  <verify>
    <automated>npm list tailwindcss lucide-vue-next date-fns pinia axios && [ -f tailwind.config.js ]</automated>
  </verify>
  <done>Project initialized with all dependencies and Tailwind CSS configured.</done>
</task>

<task type="auto">
  <name>Task 2: Define Data Models & Municipality Data</name>
  <files>src/types/holiday.ts, src/data/municipalities.json</files>
  <action>
    Define TypeScript interfaces for:
    - `Holiday`: type (fixed/mobile), day, month, name.
    - `Municipality`: id (ISO-like), name, district, holiday (Holiday object).
    - `CalendarDay`: date, isWeekend, isHoliday, isVacation, holidayName.

    Create `src/data/municipalities.json` with a sample of at least 5 municipalities (e.g., Lisboa, Porto, Almada, Coimbra, Braga) using the schema from RESEARCH.md. Add a placeholder comment or note that the full 308 list will be populated in Phase 2.
  </action>
  <verify>
    <automated>test -f src/types/holiday.ts && test -f src/data/municipalities.json</automated>
  </verify>
  <done>Core types defined and municipality JSON structure established.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize Config Store</name>
  <files>src/store/config.ts</files>
  <action>
    Create a Pinia store to manage global configuration:
    - `year` (default: 2026)
    - `selectedMunicipalityId` (default: null)
    - `maxVacationDays` (default: 22)
  </action>
  <verify>
    <automated>test -f src/store/config.ts && grep "defineStore" src/store/config.ts</automated>
  </verify>
  <done>Global configuration store initialized.</done>
</task>

</tasks>

<verification>
Check if `npm run dev` starts without errors and the project structure matches RESEARCH.md recommendations.
</verification>

<success_criteria>
- Project starts and renders a blank page with Tailwind styles working.
- `package.json` contains all specified libraries.
- Data models reflect the requirements for 308 municipalities.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding-logic/01-01-SUMMARY.md`
</output>
