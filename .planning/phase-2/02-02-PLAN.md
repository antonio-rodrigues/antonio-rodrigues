---
phase: 02-holiday-integration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/store/config.ts, src/components/ui/MunicipalitySelector.vue, src/App.vue]
autonomous: true
requirements: [FR2, FR6]
must_haves:
  truths:
    - "User can search and find any of the 308 municipalities by name"
    - "Municipality selection persists after a full browser refresh"
    - "System default is set to 'Lisboa' for new sessions"
  artifacts:
    - path: "src/components/ui/MunicipalitySelector.vue"
      provides: "Searchable dropdown for 308 concelhos"
    - path: "src/store/config.ts"
      provides: "LocalStorage persistence logic for user preferences"
  key_links:
    - from: "src/components/ui/MunicipalitySelector.vue"
      to: "src/store/config.ts"
      via: "store interaction"
      pattern: "useConfigStore"
    - from: "src/App.vue"
      to: "src/components/ui/MunicipalitySelector.vue"
      via: "component import"
      pattern: "import MunicipalitySelector from ..."
---

<objective>
Implement a searchable selection interface for all 308 Portuguese municipalities and ensure user preferences (municipality, year) are persisted across sessions.

Purpose: Allow users to customize the calendar for their specific location.
Output: A searchable select component and a persistent config store.
</objective>

<execution_context>
@/Users/arodri06/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/arodri06/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/store/config.ts
@src/data/municipalities.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LocalStorage Persistence to Config Store</name>
  <files>src/store/config.ts</files>
  <action>
    Update `src/store/config.ts` to sync state with `localStorage`:
    - On store initialization, load `year`, `selectedMunicipalityId`, and `maxVacationDays` from `localStorage`.
    - Use Pinia's `$subscribe` or a custom watcher to save changes back to `localStorage` whenever they are modified.
    - Default `selectedMunicipalityId` to 'lisboa' if not set.
  </action>
  <verify>
    <automated># Manual check: Set value in console, refresh, check if value persists</automated>
  </verify>
  <done>Config store values are persistent across page reloads.</done>
</task>

<task type="auto">
  <name>Task 2: Build Searchable MunicipalitySelector Component</name>
  <files>src/components/ui/MunicipalitySelector.vue</files>
  <action>
    Create a `MunicipalitySelector.vue` component:
    - Use a text input for searching and a dropdown list of 308 municipalities.
    - Filter the list based on user input (name or district).
    - Style with Tailwind: Mobile-friendly (full width on small screens), clean hover states.
    - Bind `v-model` or equivalent to `configStore.selectedMunicipalityId`.
    - Use `lucide-vue-next` Search icon.
  </action>
  <verify>
    <automated># Manual verification: Type 'Porto' in search, check if list filters correctly</automated>
  </verify>
  <done>Searchable municipality selector component is functional and styled.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Selector into Main Layout</name>
  <files>src/App.vue</files>
  <action>
    Integrate `MunicipalitySelector` into the top of `App.vue`:
    - Place it in a header or control bar above the `YearGrid`.
    - Ensure it is responsive and centers/aligns well with the title "Planeador de Férias 2026".
    - Add a label "Município:" for clarity.
  </action>
  <verify>
    <automated># Visual verification of the selector position and styling</automated>
  </verify>
  <done>Municipality selector is visible and integrated into the app layout.</done>
</task>

</tasks>

<success_criteria>
- Searchable select filters 308 items smoothly.
- Selecting 'Porto' updates the global state.
- Refreshing the page keeps 'Porto' selected.
- Layout is responsive and clean.
</success_criteria>

<output>
After completion, create `.planning/phases/02-holiday-integration/02-02-SUMMARY.md`
</output>
