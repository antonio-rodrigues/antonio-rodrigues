---
phase: 02-holiday-integration
plan: 03
type: execute
wave: 3
depends_on: ["02-02", "02-01"]
files_modified: [src/components/calendar/DayCell.vue, src/components/calendar/MonthCard.vue, src/components/calendar/YearGrid.vue, src/composables/useCalendar.ts]
autonomous: true
requirements: [FR2, FR3]
must_haves:
  truths:
    - "Holidays are highlighted in the calendar with a distinct visual style (pink/red)"
    - "Weekends are clearly differentiated from workdays"
    - "Hovering on a holiday shows its specific name to the user"
    - "Selecting a new municipality immediately updates the holidays shown in the 12-month grid"
  artifacts:
    - path: "src/components/calendar/DayCell.vue"
      provides: "Visual styling for holidays and weekends"
    - path: "src/components/calendar/YearGrid.vue"
      provides: "Integration with useHolidays to pass data to subcomponents"
  key_links:
    - from: "src/components/calendar/YearGrid.vue"
      to: "src/composables/useHolidays.ts"
      via: "hook usage"
      pattern: "useHolidays"
    - from: "src/components/calendar/MonthCard.vue"
      to: "src/components/calendar/DayCell.vue"
      via: "prop passing"
      pattern: "isHoliday"
---

<objective>
Update the calendar UI to visually represent holidays and weekends, and provide tooltips for holiday descriptions.

Purpose: Finalize the holiday integration by making them visible and interactive in the calendar grid.
Output: Enhanced calendar components with holiday logic and tooltips.
</objective>

<execution_context>
@/Users/arodri06/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/arodri06/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/composables/useHolidays.ts
@src/components/calendar/DayCell.vue
@src/components/calendar/MonthCard.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Connect YearGrid to useHolidays</name>
  <files>src/components/calendar/YearGrid.vue, src/composables/useCalendar.ts</files>
  <action>
    - Update `YearGrid.vue` to call `useHolidays()` and reactive the `holidays` map.
    - Pass the `holidays` map as a prop to each `MonthCard`.
    - Modify `useCalendar.ts` to accept the `holidays` map in `getYearData` to correctly set `isHoliday` and `holidayName` for each day.
  </action>
  <verify>
    <automated># Manual check: Verify Jan 1st is marked as holiday in the data passed to MonthCard</automated>
  </verify>
  <done>Holiday data is correctly propagated from useHolidays to the grid components.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance DayCell Styling and Tooltips</name>
  <files>src/components/calendar/DayCell.vue</files>
  <action>
    - Update `DayCell.vue` to accept `isHoliday` and `holidayName` props.
    - Style: Add a distinct background (e.g., `bg-red-50` or `bg-pink-100`) and text color for holidays.
    - Tooltip: Add a native `title` attribute or a simple custom tooltip to show `holidayName` on hover.
    - Weekend styling: Ensure weekends remain distinct (can combine with holiday styling if both apply).
  </action>
  <verify>
    <automated># Visual verification: Hover on Jan 1st, check if "Ano Novo" appears</automated>
  </verify>
  <done>DayCell visually indicates holidays and shows their names on hover.</done>
</task>

<task type="auto">
  <name>Task 3: Refine MonthCard Layout</name>
  <files>src/components/calendar/MonthCard.vue</files>
  <action>
    - Ensure `MonthCard` correctly passes holiday information from the grid data down to `DayCell`.
    - Verify that empty leading cells don't trigger holiday/weekend styling.
  </action>
  <verify>
    <automated># Visual verification: Check Feb 2026 starts correctly and has no ghost holidays in empty cells</automated>
  </verify>
  <done>MonthCard correctly renders the updated DayCells with holiday data.</done>
</task>

</tasks>

<success_criteria>
- All national holidays (13) are visible in the calendar.
- The municipal holiday for the selected municipality is visible.
- Hovering over June 13th (when Lisbon is selected) shows "Santo António".
- Weekends are visually highlighted (e.g., light gray background).
- Changing the municipality updates the holiday indicators instantly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-holiday-integration/02-03-SUMMARY.md`
</output>
