---
phase: 02-holiday-integration
plan: 01
type: execute
wave: 1
depends_on: ["01-03"]
files_modified: [src/data/municipalities.json, src/utils/holiday-utils.ts, src/composables/useHolidays.ts]
autonomous: true
requirements: [FR2]
must_haves:
  truths:
    - "Application successfully retrieves national holidays from Nager.Date API for 2026"
    - "System identifies 308 unique municipalities and their holiday rules"
    - "Mobile holidays (Easter, Ascension, etc.) are accurately calculated for the target year"
    - "The holiday data source provides a unified view of all active holidays for the selected location"
  artifacts:
    - path: "src/data/municipalities.json"
      provides: "Full list of 308 Portuguese municipalities and their holidays"
    - path: "src/utils/holiday-utils.ts"
      provides: "Utility to calculate Easter and mobile holidays"
    - path: "src/composables/useHolidays.ts"
      provides: "Composable to fetch and merge national and municipal holidays"
  key_links:
    - from: "src/composables/useHolidays.ts"
      to: "src/data/municipalities.json"
      via: "import"
      pattern: "import municipalities from '../data/municipalities.json'"
    - from: "src/composables/useHolidays.ts"
      to: "src/utils/holiday-utils.ts"
      via: "import"
      pattern: "import { getEaster, ... } from '../utils/holiday-utils'"
---

<objective>
Implement the core holiday data layer by integrating the Nager.Date API for national holidays and a complete local dataset of 308 Portuguese municipalities.

Purpose: Provides the data backend for holiday visualization and vacation day calculations.
Output: A robust `useHolidays` composable, a complete `municipalities.json` dataset, and accurate mobile holiday calculation utilities.
</objective>

<execution_context>
@/Users/arodri06/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/arodri06/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/holiday.ts
@src/store/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate Full 308 Municipalities Data</name>
  <files>src/data/municipalities.json</files>
  <action>
    Update `src/data/municipalities.json` with the full list of 308 Portuguese municipalities.
    Each entry must include:
    - `id`: Unique identifier (e.g., "lisboa", "porto").
    - `name`: Full name (e.g., "Lisboa").
    - `district`: District name (e.g., "Lisboa").
    - `holiday`: { type: "fixed" | "mobile", day: number, month: number, name: string, offset?: number, base?: "easter" }.
    
    Ensure mobile holidays like "Quinta-feira da Ascensão" (Easter + 39 days) or "Segunda-feira de Pentecostes" (Easter + 50 days) are correctly defined for municipalities that observe them (e.g., Almada, Alenquer).
  </action>
  <verify>
    <automated>grep -c "\"id\":" src/data/municipalities.json | grep 308</automated>
  </verify>
  <done>municipalities.json contains 308 unique entries with valid holiday rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Easter and Mobile Holiday Utilities</name>
  <files>src/utils/holiday-utils.ts, src/utils/holiday-utils.spec.ts</files>
  <behavior>
    - Test: getEaster(2026) -> April 5, 2026.
    - Test: getAscension(2026) -> May 14, 2026 (Easter + 39).
    - Test: getPentecostMonday(2026) -> May 25, 2026 (Easter + 50).
    - Test: getCorpusChristi(2026) -> June 4, 2026 (Easter + 60).
  </behavior>
  <action>
    Create `src/utils/holiday-utils.ts` and implement functions to calculate:
    - Easter Sunday (Meeus/Jones/Butcher algorithm).
    - Ascension (Easter + 39).
    - Pentecost Monday (Easter + 50).
    - Corpus Christi (Easter + 60).
    - Carnaval (Easter - 47).
    - Good Friday (Easter - 2).
    - Easter Monday (Easter + 1).
  </action>
  <verify>
    <automated>npm test src/utils/holiday-utils.spec.ts</automated>
  </verify>
  <done>Date utilities for mobile holidays are accurate and verified for 2026.</done>
</task>

<task type="auto">
  <name>Task 3: Create useHolidays Composable</name>
  <files>src/composables/useHolidays.ts</files>
  <action>
    Implement `useHolidays` composable:
    - Fetch national holidays from Nager.Date API (`https://date.nager.at/api/v3/publicholidays/{year}/PT`).
    - Load municipal holiday based on `selectedMunicipalityId` from `configStore`.
    - Resolve mobile municipal holidays using `holiday-utils.ts`.
    - Provide a reactive `holidays` object (Record<string, Holiday>) where key is "YYYY-MM-DD".
    - Handle API loading states and basic error handling/caching.
  </action>
  <verify>
    <automated># Manual verification via basic console.log in App.vue showing 2026-06-13 as "Santo António" for Lisbon</automated>
  </verify>
  <done>useHolidays hook provides merged, accurate holiday data for any year and municipality.</done>
</task>

</tasks>

<success_criteria>
- `municipalities.json` is a complete dataset of 308 concelhos.
- `getEaster(2026)` returns April 5th.
- `useHolidays` successfully merges national API data with local municipal rules.
- Mobile holidays are correctly resolved for the target year (2026).
</success_criteria>

<output>
After completion, create `.planning/phases/02-holiday-integration/02-01-SUMMARY.md`
</output>
